import React from 'react'
import Photo from '../Photo/Photo'

const Favourites = () => {
  return (
    <h1>favirout</h1>
    // <section>{
    //   favoritePhotos.map((image,index)=>{
    //     return(
    //       <Photo
    //         key={index}
    //         {...image}
    //         isFavourite={true}
    //         oncFvouriteClick= {()=>handleRemoveFavorite(image)}
    //       >
    //       <span>Added to favourit</span>
    //       </Photo>
    //     )
    //   })
    // }</section>
  )
}

export default Favourites